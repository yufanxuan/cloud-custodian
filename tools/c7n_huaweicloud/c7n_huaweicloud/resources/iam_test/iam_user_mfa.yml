policies:
  - name: mfa-enabled-users
    resource: huaweicloud.iam-user
    filters:
      - or:
        - type: mfa-device
          key: "length(mfa_devices || `[]`)"
          value: 0
          op: eq
        - type: mfa-device
          key: "enabled"
          value: false
          op: any